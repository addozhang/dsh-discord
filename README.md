# @addozhang/dsh-discord

Discord-first adapter for DeepSeek Harness.

The implementation follows the OpenSpec change at `openspec/changes/build-discord-native-adapter/` and is currently under development.

## Development

```sh
pnpm install --ignore-scripts
pnpm test
pnpm typecheck
pnpm lint
pnpm build
```
