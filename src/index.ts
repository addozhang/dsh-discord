/** Stable Cordis plugin name for diagnostics. */
export const name = 'dsh-discord'

/**
 * The package scaffold intentionally mounts no runtime effects yet.
 * OpenSpec task 1.3 adds the validated DSH Host dependency boundary.
 */
export function apply(): void {}
